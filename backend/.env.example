# backend/.env.example
# Copia a backend/.env (no commitear). Valores de ejemplo, NO reales.

PORT=3000
NODE_ENV=development

# MongoDB (preparado, aún sin features). Si se omite, la API arranca sin DB.
MONGODB_URI=mongodb://localhost:27017/desafio-26

# Auth (no implementado todavía; reservado para futuras ramas)
JWT_SECRET=change_me
JWT_EXPIRES_IN=7d

# CORS / cliente
CLIENT_URL=http://localhost:5173
