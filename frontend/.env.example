# frontend/.env.example
# Copia a frontend/.env (no commitear). Solo variables VITE_* llegan al cliente.

VITE_API_URL=http://localhost:3000/api

# Google OAuth (Client ID "Aplicación web" de Google Cloud Console; público, no es secreto).
VITE_GOOGLE_CLIENT_ID=TU_GOOGLE_CLIENT_ID
